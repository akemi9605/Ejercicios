//ejercicio ahora en la que tiene que contar cuantas letras y vocales hay de la cadena de texto

function contarLetras (cadena) {
	var texto = cadena.split[""];
}

function cuantasVocales (cadenaDeTexto) {
	var numeroDeVocales = 0;
	for (i = 0; i < )
}

//Haz una función que reciba dos números y te devuelva la suma de ambos;
function recibirNumero (a, b) {
	var devolver = a + b;
	console.log(devolver);
}
recibirNumero(3, 8);

//Haz una función que reciba la hora que es (solo la hora, por ej 18) y te diga si es por la tarde o por la mañana.
function recibirHora (hora) {
	if ( hora > 12 ) {
	console.log("Es por la tarde"); 
	}
	else {
	console.log("Es por la mañana");
	}
}
recibirHora(22);

//Haz una función que reciba un numero de minutos, por ejemplo (1231) y te devuelva cuántas horas es eso. Por ejemplo 140 minutos serán 2 horas

function calcularHoras (contarMinutos) {
	var horas = contarMinutos / 60;
	console.log(Math.round(horas));
}
calcularHoras (140);